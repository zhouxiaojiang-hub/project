---
name: dev-server
description: 启动开发服务器（后端、商户端、消费者端）
---

# 街头地摊推广系统 - 开发服务器启动

## 后端启动

```bash
cd backend
# 激活虚拟环境（如果有）
# source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate     # Windows

# 启动FastAPI服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

访问: http://localhost:8000
API文档: http://localhost:8000/docs

## 商户端启动

```bash
cd frontend-merchant
npm run dev
```

访问: http://localhost:3001

## 消费者端启动

```bash
cd frontend-consumer
npm run dev
```

访问: http://localhost:3002

## 全部启动（推荐使用终端分屏）

1. 打开三个终端窗口
2. 分别运行上述三个命令
3. 确保所有服务正常启动

## 常见问题

### 端口被占用
- 后端: 修改端口 `--port 8001`
- 前端: 修改 vite.config.ts 中的 port

### 依赖未安装
- 后端: `pip install -r requirements.txt`
- 前端: `npm install`

### 数据库文件不存在
- 自动创建: 首次启动后端会自动创建 SQLite 文件
